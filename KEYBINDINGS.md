# 键位与操作

### 快捷键

按键分两种模式。**输入框获得焦点时**（搜索框、新建歌单输入框），字母键会原样插入文本
而不是触发快捷键——否则就没法搜带 `q`、`j` 的歌名了。按 `Esc` 退出输入态。

**全局与导航**

| 按键 | 功能 |
|---|---|
| `q` / `Q` | 退出 / 强制退出 |
| `?` / `F1` | 帮助面板 |
| `\` | 显示 / 隐藏侧边栏 |
| `1`–`9`、`0` | 切换前 10 个标签页（列表内则是跳到第 N 项） |
| `,` | 打开设置页（它排在第 12 位，数字键够不到） |
| `Tab` / `Shift+Tab` | 轮转焦点区域 |
| `j` / `k` 或 `↓` / `↑` | 上下移动 |
| `g` / `G` | 跳到首行 / 末行 |
| `PgUp` / `PgDn` | 翻页 |
| `Enter` | 播放选中歌曲 / 进入 |
| `Esc` | 返回上一层 |

**播放**

| 按键 | 功能 |
|---|---|
| `Space` | 播放 / 暂停 |
| `n` / `p` | 下一首 / 上一首 |
| `←` / `→` | 快退 / 快进 5 秒 |
| `+` / `-` | 音量 ±5% |
| `m` | 静音开关 |
| `r` | 循环播放模式（顺序 → 列表循环 → 单曲循环 → 随机） |
| `l` | 歌词面板开关 |
| `[` / `]` | 歌词延后 / 提前 100 ms |

**队列与云端**

| 按键 | 功能 |
|---|---|
| `;` | 打开**歌曲右键菜单**（也可用鼠标右键） |
| `a` / `i` | 加入队列 / 插播下一首 |
| `A` | 把当前列表**全部**加入队列 |
| `x` / `X` | 从队列移除选中 / 清空队列（需确认） |
| `o` | 正序 ↔ 倒序（默认倒序，最后一首在最上） |
| `s` / `S` | 收藏单曲到云端 / 同步整个队列 |
| `d` / `D` | 从云端歌单移除 / 删除歌单（需确认） |
| `N` | 新建云端歌单 |
| `L` | 扫码登录 |
| `R` | 刷新当前列表 |
| `/` | 聚焦搜索框 |
| `b` / `c` | 排行榜 / 云端歌单 |
| `f` | 歌手地区筛选（在歌手页） |
| `C` | 清空音频缓存（需确认） |
| `M` | 搜索结果加载更多（追加下一页） |

> `x` / `X` 作用于播放队列，需要先把焦点切到队列面板（按 `Tab` 轮转）。
> `d` / `D` 作用于云端歌单，需要先在「云端」标签页选中一个歌单。

### 鼠标操作

| 操作 | 效果 |
|---|---|
| 单击列表行 | 选中该行并把焦点切到该面板 |
| 双击列表行 | 激活（等同 `Enter`） |
| **右键列表行** | 弹出**歌曲菜单**（等同 `;`），见下方 |
| 滚轮 | 在光标所在列表上移动选中 |
| 单击侧边栏标签 | 切换标签页（含数字键够不到的音源与设置） |
| 单击进度条 | 跳转到对应时间 |
| 单击设置项 | 选中；**再点一次**改值 |

### 歌曲右键菜单

对光标所在的歌曲按 `;` 或**鼠标右键**：

```
╭──《稻香》────────────╮
│ > 播放        Enter  │
│   加入队列      a    │
│   插播下一首    i    │
│   收藏到云端    s    │
│   下载到本地    W    │
╰──────────────────────╯
```

每一项都标出了对应的键位——菜单只是把键位集中到光标处，**不是另一套交互**，
所以菜单里不会出现没有对应键位的操作。在播放队列里时，「加入队列」会换成
「从队列移除」（对已在队列里的歌再加一次没有意义）。

`↑` `↓` 选择、`Enter` 执行、`1`-`5` 直接选中、`Esc` 关闭。
